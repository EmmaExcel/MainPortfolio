import {
  __publicField
} from "./chunk-2GTGKKMZ.js";

// node_modules/@emmaexcel/shakecursor/dist/overlay.js
function e(e3, t2, n2) {
  return Math.min(Math.max(e3, t2), n2);
}
function t(e3) {
  return e3.replace(/\s+/g, " ").trim();
}
function n(e3, t2 = []) {
  return t2.some((t3) => {
    try {
      return e3.matches(t3) || !!e3.closest(t3);
    } catch {
      return false;
    }
  });
}
function r() {
  return /Mac|iPhone|iPad|iPod/i.test(window.navigator.platform);
}
function i(e3, t2 = 50) {
  let n2 = null;
  return (...r2) => {
    n2 && clearTimeout(n2), n2 = setTimeout(() => {
      e3(...r2), n2 = null;
    }, t2);
  };
}
var a = "color.background-color.background.font-size.font-weight.font-family.line-height.letter-spacing.text-align.padding.padding-top.padding-right.padding-bottom.padding-left.margin.margin-top.margin-right.margin-bottom.margin-left.border.border-radius.width.height.max-width.min-height.display.flex-direction.align-items.justify-content.gap.opacity.transform.box-shadow.text-shadow".split(".");
var o = "shakecursor-edits";
var s = [];
var c = -1;
function l() {
  let e3 = document.getElementById(o);
  return e3 || (e3 = document.createElement("style"), e3.id = o, e3.setAttribute("data-shakecursor", "true"), document.head.appendChild(e3)), e3;
}
function u(e3) {
  let t2 = document.getElementById(o);
  if (!t2)
    return "";
  let n2 = t2.sheet;
  if (!n2)
    return "";
  for (let t3 = 0; t3 < n2.cssRules.length; t3++) {
    let r2 = n2.cssRules[t3];
    if (r2.selectorText === e3)
      return r2.cssText;
  }
  return "";
}
function d(e3, t2) {
  let n2 = l().sheet;
  for (let t3 = n2.cssRules.length - 1; t3 >= 0; t3--)
    n2.cssRules[t3].selectorText === e3 && n2.deleteRule(t3);
  if (Object.keys(t2).length === 0)
    return;
  let r2 = Object.entries(t2).map(([e4, t3]) => `${e4}: ${t3} !important`).join("; ");
  n2.insertRule(`${e3} { ${r2} }`, n2.cssRules.length);
}
function f(e3) {
  if (e3.id)
    return `#${CSS.escape(e3.id)}`;
  let t2 = [], n2 = e3;
  for (; n2 && n2 !== document.documentElement; ) {
    let e4 = n2.tagName.toLowerCase();
    if (n2.id) {
      t2.unshift(`#${CSS.escape(n2.id)}`);
      break;
    }
    if (n2.className) {
      let t3 = Array.from(n2.classList).filter((e5) => !e5.startsWith("ai-") && e5.length > 0).slice(0, 2).map((e5) => `.${CSS.escape(e5)}`).join("");
      e4 += t3;
    }
    let r2 = n2.parentElement;
    if (r2) {
      let t3 = Array.from(r2.children).filter((e5) => e5.tagName === n2.tagName);
      if (t3.length > 1) {
        let r3 = t3.indexOf(n2) + 1;
        e4 += `:nth-child(${r3})`;
      }
    }
    t2.unshift(e4), n2 = n2.parentElement;
  }
  return t2.join(" > ");
}
function p(e3) {
  let t2 = window.getComputedStyle(e3), n2 = {};
  for (let e4 of a) {
    let r2 = t2.getPropertyValue(e4);
    r2 && (n2[e4] = r2);
  }
  return n2;
}
function m(e3) {
  let t2 = u(e3.selector);
  d(e3.selector, e3.css);
  let n2 = u(e3.selector);
  s.splice(c + 1), s.push({
    selector: e3.selector,
    previousCSS: t2,
    nextCSS: n2,
    description: e3.description
  }), c = s.length - 1;
}
function h() {
  if (c < 0)
    return null;
  let e3 = s[c];
  c--;
  let t2 = l().sheet;
  for (let n2 = t2.cssRules.length - 1; n2 >= 0; n2--)
    t2.cssRules[n2].selectorText === e3.selector && t2.deleteRule(n2);
  return e3.previousCSS && t2.insertRule(e3.previousCSS, t2.cssRules.length), `Undid: ${e3.description}`;
}
function g() {
  return c >= 0;
}
function _() {
  let e3 = document.getElementById(o);
  if (!e3)
    return "";
  let t2 = e3.sheet;
  return t2 ? Array.from(t2.cssRules).map((e4) => e4.cssText).join("\n") : "";
}
function v(e3) {
  if (e3.rangeCount === 0)
    return null;
  let t2 = e3.getRangeAt(0), n2 = t2.getBoundingClientRect();
  return n2.width > 0 || n2.height > 0 ? n2 : t2.getClientRects()[0] ?? null;
}
function y(e3) {
  return {
    rect: e3,
    url: window.location.href,
    title: document.title
  };
}
function b(e3) {
  let n2 = t(e3.innerText ?? ""), r2 = e3.tagName.toLowerCase(), i2 = e3.getAttribute("aria-label"), a2 = e3.getAttribute("title"), o2 = [
    i2,
    a2,
    n2
  ].find(Boolean);
  return {
    label: o2 ? `${r2}: ${o2.slice(0, 64)}` : r2,
    data: void 0,
    mimeType: void 0,
    content: [
      `Selected element: <${r2}>`,
      i2 ? `ARIA label: ${i2}` : "",
      a2 ? `Title: ${a2}` : "",
      n2 ? `Visible text: ${n2.slice(0, 5e3)}` : ""
    ].filter(Boolean).join("\n")
  };
}
function x(e3) {
  let t2 = e3.currentSrc || e3.src, n2 = e3.alt || "No alt text";
  return new Promise((r2) => {
    let i2 = {
      label: `image: ${n2.slice(0, 64)}`,
      data: void 0,
      mimeType: void 0,
      alt: n2,
      source: t2,
      content: [
        "Selected image",
        `Alt text: ${n2}`,
        `Source: ${t2}`,
        `Rendered size: ${Math.round(e3.width)}x${Math.round(e3.height)}`,
        `Natural size: ${e3.naturalWidth}x${e3.naturalHeight}`
      ].join("\n")
    };
    try {
      let t3 = document.createElement("canvas");
      t3.width = e3.naturalWidth || e3.width || 100, t3.height = e3.naturalHeight || e3.height || 100;
      let n3 = t3.getContext("2d");
      if (n3) {
        n3.drawImage(e3, 0, 0);
        let [a3, o2] = t3.toDataURL("image/jpeg", 0.8).split(",");
        i2.data = o2, i2.mimeType = a3.split(":")[1].split(";")[0], r2(i2);
        return;
      }
    } catch {
    }
    let a2 = new Image();
    a2.crossOrigin = "anonymous", a2.onload = () => {
      try {
        let e4 = document.createElement("canvas");
        e4.width = a2.naturalWidth || a2.width || 100, e4.height = a2.naturalHeight || a2.height || 100;
        let t3 = e4.getContext("2d");
        if (t3) {
          t3.drawImage(a2, 0, 0);
          let [n3, r3] = e4.toDataURL("image/jpeg", 0.8).split(",");
          i2.data = r3, i2.mimeType = n3.split(":")[1].split(";")[0];
        }
      } catch (e4) {
        console.warn("CORS fallback failed to export canvas:", e4);
      }
      r2(i2);
    }, a2.onerror = () => {
      console.warn("CORS fallback failed to load image copy"), r2(i2);
    }, t2.startsWith("data:") ? a2.src = t2 : a2.src = `${t2}${t2.includes("?") ? "&" : "?"}crossorigin-bypass=true`;
  });
}
function S(e3, t2) {
  if (n(e3, t2.blockedSelectors))
    return false;
  let r2 = e3.getBoundingClientRect(), i2 = window.innerWidth * window.innerHeight;
  return !(r2.width * r2.height > i2 * 0.5 || t2.allowedSelectors.length > 0 && !n(e3, t2.allowedSelectors));
}
function C(e3) {
  let n2 = (t2) => {
    if (!e3.isActive())
      return;
    let n3 = t2.target;
    if (!(n3 instanceof HTMLElement) || e3.isOverlayElement(n3)) {
      e3.onHover(null);
      return;
    }
    if (!S(n3, e3.config)) {
      e3.onHover(null);
      return;
    }
    e3.onHover(n3.getBoundingClientRect());
  }, r2 = () => {
    !e3.isActive() || !e3.config.text || window.setTimeout(() => {
      var _a;
      let n3 = window.getSelection(), r3 = t((n3 == null ? void 0 : n3.toString()) ?? "");
      if (!n3 || !r3)
        return;
      let i3 = v(n3), a2 = (_a = n3.anchorNode) == null ? void 0 : _a.parentElement;
      !i3 || !a2 || !S(a2, e3.config) || e3.onSelection({
        kind: "text",
        label: "highlighted text",
        content: r3,
        ...y(i3)
      });
    }, 0);
  }, i2 = async (n3) => {
    var _a;
    if (!e3.isActive())
      return;
    let r3 = n3.target;
    if (!(r3 instanceof HTMLElement) || e3.isOverlayElement(r3) || !S(r3, e3.config) || t(((_a = window.getSelection()) == null ? void 0 : _a.toString()) ?? ""))
      return;
    let i3 = r3 instanceof HTMLImageElement ? r3 : r3.querySelector("img"), a2 = !!i3;
    if (a2 && !e3.config.images || !a2 && !e3.config.elements)
      return;
    n3.preventDefault(), n3.stopPropagation();
    let o2 = i3 ? await x(i3) : b(r3), s2 = i3 ? i3.getBoundingClientRect() : r3.getBoundingClientRect(), c2 = i3 ?? r3;
    e3.onSelection({
      kind: i3 ? "image" : "element",
      label: o2.label,
      content: o2.content,
      data: o2.data,
      mimeType: o2.mimeType,
      alt: o2.alt,
      source: o2.source,
      element: c2,
      cssSelector: f(c2),
      computedStyles: p(c2),
      ...y(s2)
    });
  };
  return document.addEventListener("pointerover", n2), document.addEventListener("mouseup", r2), document.addEventListener("click", i2, true), () => {
    document.removeEventListener("pointerover", n2), document.removeEventListener("mouseup", r2), document.removeEventListener("click", i2, true);
  };
}
function w(e3) {
  let t2 = e3.windowMs ?? 650, n2 = e3.cooldownMs ?? 1200, r2 = e3.minSamples ?? 8, a2 = e3.minReversals ?? 4, o2 = e3.minDistance ?? 360, s2 = e3.minDeltaX ?? 18, c2 = e3.hysteresisEnabled ?? true, l2 = e3.lastShakeAt ?? 0, u2 = [], d2 = i(() => {
    p2();
  }, 5), f2 = () => c2 ? performance.now() - l2 < n2 : false, p2 = () => {
    let t3 = performance.now();
    if (u2.length < r2 || f2())
      return;
    let n3 = 0, i2 = 0, c3 = 0, d3 = false;
    for (let e4 = 1; e4 < u2.length; e4 += 1) {
      let t4 = u2[e4].x - u2[e4 - 1].x, r3 = u2[e4].y - u2[e4 - 1].y;
      if (i2 += Math.hypot(t4, r3), !(Math.abs(t4) >= s2))
        continue;
      let a3 = Math.sign(t4);
      c3 !== 0 && a3 !== c3 && (n3 += 1), c3 = a3, Math.abs(t4) > s2 * 2 && (d3 = true);
    }
    n3 >= a2 && i2 > o2 && d3 && (l2 = t3, u2 = [], e3.onShake());
  }, m2 = (e4) => {
    let n3 = performance.now();
    u2.push({
      x: e4.clientX,
      y: e4.clientY,
      time: n3
    }), u2 = u2.filter((e5) => n3 - e5.time < t2), d2();
  };
  return document.addEventListener("pointermove", m2, { passive: true }), () => {
    document.removeEventListener("pointermove", m2);
  };
}
var T = "http://localhost:11434/api/chat";
var E = "qwen3-coder:480b-cloud";
var D = "https://shakeai.onrender.com";
async function O(e3, t2) {
  var _a, _b, _c, _d, _e;
  let n2 = e3.provider ?? "ollama", r2 = e3.endpoint ?? T, i2 = e3.model ?? E;
  if (n2 === "custom") {
    let n3 = r2.includes("/chat/completions") || r2.includes("/v1/chat"), a3;
    a3 = n3 ? {
      model: e3.model || i2,
      messages: [{
        role: "system",
        content: "You are an in-page AI assistant. Use the selected website context to answer or transform content. Be direct and practical."
      }, {
        role: "user",
        content: k(t2)
      }]
    } : t2;
    let o3 = await fetch(r2, {
      method: "POST",
      headers: {
        "Content-Type": "application/json",
        ...e3.headers
      },
      body: JSON.stringify(a3)
    });
    if (!o3.ok)
      throw Error(`Custom endpoint returned ${o3.status}`);
    let s2 = await o3.json();
    return typeof s2 == "string" ? s2 : s2 && typeof s2 == "object" && ((_c = (_b = (_a = s2.choices) == null ? void 0 : _a[0]) == null ? void 0 : _b.message) == null ? void 0 : _c.content) ? s2.choices[0].message.content.trim() : s2 && typeof s2 == "object" && "answer" in s2 && typeof s2.answer == "string" ? s2.answer : s2 && typeof s2 == "object" && "content" in s2 && typeof s2.content == "string" ? s2.content : JSON.stringify(s2, null, 2);
  }
  let a2 = await fetch(r2, {
    method: "POST",
    headers: {
      "Content-Type": "application/json",
      ...e3.headers
    },
    body: JSON.stringify({
      model: i2,
      stream: false,
      messages: [{
        role: "system",
        content: "You are an in-page AI assistant. Use the selected website context to answer or transform content. Be direct and practical."
      }, {
        role: "user",
        content: k(t2)
      }]
    })
  });
  if (!a2.ok)
    throw Error(`Ollama returned ${a2.status}`);
  let o2 = await a2.json();
  if (o2.error)
    throw Error(o2.error);
  return ((_e = (_d = o2.message) == null ? void 0 : _d.content) == null ? void 0 : _e.trim()) || "No response returned.";
}
function k(e3) {
  return [
    `Page title: ${e3.selection.title}`,
    `Page URL: ${e3.selection.url}`,
    `Selection type: ${e3.selection.kind}`,
    `Selection label: ${e3.selection.label}`,
    "Selected context:",
    e3.selection.content,
    "",
    `User request: ${e3.question}`
  ].join("\n");
}
async function A(e3) {
  let t2 = await fetch(`${e3.apiBaseUrl ?? D}/v1/ask`, {
    method: "POST",
    headers: {
      "Content-Type": "application/json",
      "x-site-key": e3.siteKey
    },
    body: JSON.stringify(e3.payload)
  }), n2 = await t2.json();
  if (!t2.ok || n2.error)
    throw Error(n2.error ?? `Hosted API returned ${t2.status}`);
  return n2.answer ?? "No response returned.";
}
async function j(e3, t2) {
  var _a, _b, _c, _d, _e, _f, _g, _h, _i, _j;
  let n2 = e3.visionModel ?? "gpt-4o", r2 = e3.endpoint ?? T, i2 = r2, a2 = n2;
  if (e3.provider === "custom")
    i2 = e3.endpoint ?? r2, a2 = e3.model ?? n2;
  else if (e3.provider === "ollama") {
    let t3 = [
      "llava",
      "bakLLaVA",
      "moondream"
    ];
    (!a2 || t3.includes(a2.toLowerCase())) && (i2 = e3.endpoint ?? T, a2 ?? (a2 = t3[0]));
  } else
    e3.provider === "openrouter" && (a2 ?? (a2 = "gpt-4o"));
  if (console.log(`Using vision model: ${a2} at ${i2}`), e3.provider === "ollama") {
    let n3 = await fetch(i2, {
      method: "POST",
      headers: {
        "Content-Type": "application/json",
        ...e3.headers || {}
      },
      body: JSON.stringify({
        model: a2,
        stream: false,
        messages: [{
          role: "user",
          content: [
            "Image Analysis Request",
            `Alt text: ${t2.selection.alt}`,
            `Source: ${t2.selection.source}`,
            `Dimensions: ${(_a = t2.selection.width) == null ? void 0 : _a.toFixed(0)}x${(_b = t2.selection.height) == null ? void 0 : _b.toFixed(0)}`,
            "",
            t2.selection.data && t2.selection.mimeType ? "Image Data (Base64):" : "Visual context provided",
            t2.selection.data,
            "",
            `Your task: ${t2.question}`
          ].filter(Boolean)
        }]
      })
    });
    if (!n3.ok)
      throw Error(`Vision model returned ${n3.status}`);
    let r3 = await n3.json();
    if (r3.error)
      throw Error(r3.error);
    return ((_d = (_c = r3.message) == null ? void 0 : _c.content) == null ? void 0 : _d.trim()) || "No response returned.";
  }
  if (e3.provider === "custom" && (i2.includes("/chat/completions") || i2.includes("/v1/chat"))) {
    let n3 = "";
    t2.selection.data && (n3 = t2.selection.data.startsWith("data:") ? t2.selection.data : `data:${t2.selection.mimeType || "image/png"};base64,${t2.selection.data}`);
    let r3 = [
      "Image Analysis Request",
      t2.question ? `Question: ${t2.question}` : "",
      `Page title: ${t2.selection.title}`,
      `Page URL: ${t2.selection.url}`
    ].filter(Boolean).join("\n"), o3 = await fetch(i2, {
      method: "POST",
      headers: {
        "Content-Type": "application/json",
        ...e3.headers || {}
      },
      body: JSON.stringify({
        model: a2,
        messages: [{
          role: "user",
          content: [{
            type: "text",
            text: r3
          }, n3 ? {
            type: "image_url",
            image_url: { url: n3 }
          } : null].filter(Boolean)
        }]
      })
    });
    if (!o3.ok)
      throw Error(`OpenAI Vision API returned ${o3.status}`);
    let s3 = await o3.json();
    if ((_g = (_f = (_e = s3.choices) == null ? void 0 : _e[0]) == null ? void 0 : _f.message) == null ? void 0 : _g.content)
      return s3.choices[0].message.content.trim();
    throw Error("OpenAI Vision model returned unexpected format");
  }
  let o2 = new FormData();
  if (o2.append("model", a2), t2.selection.data && t2.selection.mimeType) {
    let e4 = t2.selection.data, n3 = atob(e4), r3 = Array(n3.length);
    for (let e5 = 0; e5 < n3.length; e5++)
      r3[e5] = n3.charCodeAt(e5);
    let i3 = new Uint8Array(r3), a3 = new Blob([i3], { type: t2.selection.mimeType });
    o2.append("image", a3, `image.${M(t2.selection.mimeType)}`);
  }
  t2.question ? o2.append("prompt", t2.question) : o2.append("prompt", "Please analyze this image and describe what you see.");
  let s2 = await fetch(e3.endpoint ?? i2, {
    method: "POST",
    headers: { ...e3.headers || {} },
    body: o2
  });
  if (!s2.ok)
    throw Error(`Vision API returned ${s2.status}`);
  let c2 = await s2.json();
  if (c2.text)
    return c2.text;
  if (c2.completion)
    return c2.completion;
  if ((_j = (_i = (_h = c2.choices) == null ? void 0 : _h[0]) == null ? void 0 : _i.message) == null ? void 0 : _j.content)
    return c2.choices[0].message.content;
  if (c2.result)
    return JSON.stringify(c2.result, null, 2);
  throw Error("Vision model returned unexpected format");
}
function M(e3) {
  return e3.split(";")[0].split("/")[1] || "jpeg";
}
async function N(e3, t2, n2) {
  var _a, _b, _c, _d;
  let r2 = e3.endpoint ?? T, i2 = e3.model ?? E, a2 = r2.includes("/chat/completions") || r2.includes("/v1/chat"), o2 = [{
    role: "system",
    content: 'You are a CSS expert and web design assistant integrated into a browser AI overlay.\nYour job is to translate natural language style requests into valid CSS property changes.\n\nYou MUST respond with ONLY a single valid JSON object in this exact format (no markdown, no explanation):\n{\n  "selector": "<the CSS selector provided>",\n  "css": {\n    "property-name": "value",\n    "another-property": "value"\n  },\n  "description": "<one sentence describing what changed>"\n}\n\nRules:\n- Only include CSS properties that need to change\n- Use standard CSS property names (kebab-case)\n- Use valid CSS values only\n- Keep changes minimal and precise\n- Do NOT include !important in values (the SDK adds it)\n- Do NOT wrap in markdown code blocks'
  }, {
    role: "user",
    content: `Element details:
- Selector: ${t2.selector}
- Tag: ${t2.tagName}
- Classes: ${t2.classes || "none"}
- Text snippet: "${t2.innerTextSnippet}"

Current computed styles:
${Object.entries(t2.computedStyles).map(([e4, t3]) => `  ${e4}: ${t3}`).join("\n")}

User request: "${n2}"

Respond with the JSON edit object only.`
  }], s2;
  if (a2 || e3.provider === "custom") {
    let t3 = await fetch(r2, {
      method: "POST",
      headers: {
        "Content-Type": "application/json",
        ...e3.headers ?? {}
      },
      body: JSON.stringify({
        model: i2,
        messages: o2
      })
    });
    if (!t3.ok)
      throw Error(`Edit model returned ${t3.status}`);
    s2 = ((_c = (_b = (_a = (await t3.json()).choices) == null ? void 0 : _a[0]) == null ? void 0 : _b.message) == null ? void 0 : _c.content) ?? "";
  } else {
    let t3 = await fetch(r2, {
      method: "POST",
      headers: {
        "Content-Type": "application/json",
        ...e3.headers ?? {}
      },
      body: JSON.stringify({
        model: i2,
        stream: false,
        messages: o2
      })
    });
    if (!t3.ok)
      throw Error(`Edit model returned ${t3.status}`);
    s2 = ((_d = (await t3.json()).message) == null ? void 0 : _d.content) ?? "";
  }
  s2 = s2.replace(/```[a-z]*\n?/gi, "").replace(/```/g, "").trim();
  try {
    let e4 = JSON.parse(s2);
    if (!e4.selector || !e4.css || typeof e4.css != "object")
      throw Error("Invalid edit result shape");
    return e4;
  } catch {
    throw Error(`AI returned invalid edit JSON: ${s2.slice(0, 200)}`);
  }
}
function P(t2) {
  let n2 = document.createElement("div");
  n2.dataset.aiOverlayRoot = "true";
  let r2 = document.createElement("style");
  r2.dataset.aiOverlayStyle = "true", r2.textContent = "\n    body.ai-overlay-active,\n    body.ai-overlay-active * {\n      cursor: none !important;\n    }\n\n    /* Inspect Mode Styles */\n    .inspect-active .kind::after {\n      content: ' [INSPECT]';\n      color: #fbbf24;\n      font-size: 0.7em;\n    }\n  ";
  let i2 = n2.attachShadow({ mode: "open" });
  document.head.append(r2), document.body.append(n2), i2.innerHTML = `
    <style>
    @import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap');

    :host {
      all: initial;
      --c-bg: #0d0f14;
      --c-surface: #13161d;
      --c-surface2: #1a1d26;
      --c-border: rgba(255,255,255,0.07);
      --c-border-bright: rgba(255,255,255,0.14);
      --c-text: #f0f2f7;
      --c-muted: #6b7280;
      --c-accent: #6366f1;
      --c-accent2: #8b5cf6;
      --c-amber: #f59e0b;
      --c-red: #f43f5e;
      --c-green: #10b981;
      --radius: 16px;
      font-family: 'Inter', ui-sans-serif, system-ui, -apple-system, sans-serif;
    }

    /* ── Keyframes ───────────────────────────────────────────── */
    @keyframes panel-in {
      from { opacity: 0; transform: translateY(14px) scale(0.96); }
      to   { opacity: 1; transform: translateY(0)    scale(1); }
    }
    @keyframes toast-in {
      from { opacity: 0; transform: translateY(8px) scale(0.94); }
      to   { opacity: 1; transform: translateY(0)   scale(1); }
    }
    @keyframes orbit {
      from { transform: rotate(0deg); }
      to   { transform: rotate(360deg); }
    }
    @keyframes cursor-breathe {
      0%,100% { filter: drop-shadow(0 0 6px rgba(99,102,241,0.8)); }
      50%      { filter: drop-shadow(0 0 18px rgba(139,92,246,1)); }
    }
    @keyframes dot-pulse {
      0%,100% { box-shadow: 0 0 0 0 rgba(99,102,241,0.6); }
      50%      { box-shadow: 0 0 0 5px rgba(99,102,241,0); }
    }
    @keyframes shimmer {
      0%   { background-position: -300px 0; }
      100% { background-position:  300px 0; }
    }
    @keyframes answer-in {
      from { opacity: 0; transform: translateY(6px); }
      to   { opacity: 1; transform: translateY(0); }
    }

    /* ── Reset ───────────────────────────────────────────────── */
    .toast,.hover,.panel,.head,.kind,.label,.close,.content-area,
    .answer,.footer,form,textarea,.submit,.error,.mode-tabs,
    .tab-btn,.edit-controls,.edit-btn,.input-row,.hint {
      all: initial;
      box-sizing: border-box;
      font-family: 'Inter', ui-sans-serif, system-ui, sans-serif;
    }

    /* ── Animated AI Cursor ──────────────────────────────────── */
    .custom-cursor {
      position: fixed;
      left: 0; top: 0;
      width: 32px; height: 32px;
      pointer-events: none;
      z-index: 2147483647;
      display: none;
      transform: translate(-50%, -50%);
      animation: cursor-breathe 2s ease-in-out infinite;
    }
    .custom-cursor.active { display: block; }
    .cursor-orbit {
      position: absolute;
      inset: 0;
      border-radius: 50%;
      border: 1.5px solid transparent;
      border-top-color: var(--c-accent);
      border-right-color: var(--c-accent2);
      animation: orbit 1.4s linear infinite;
    }
    .cursor-body {
      position: absolute;
      inset: 6px;
      border-radius: 50%;
      background: radial-gradient(circle at 35% 35%, rgba(139,92,246,0.9), rgba(99,102,241,0.7));
      box-shadow: 0 0 12px rgba(99,102,241,0.6);
    }
    .cursor-dot {
      position: absolute;
      width: 5px; height: 5px;
      border-radius: 50%;
      background: #fff;
      top: 50%; left: 50%;
      transform: translate(-50%,-50%);
    }
    .cursor-trail {
      position: fixed;
      width: 8px; height: 8px;
      border-radius: 50%;
      background: rgba(139,92,246,0.5);
      pointer-events: none;
      z-index: 2147483646;
      display: none;
      transform: translate(-50%,-50%);
      transition: left 0.12s ease-out, top 0.12s ease-out;
    }
    .cursor-trail.active { display: block; }

    /* ── Toast ───────────────────────────────────────────────── */
    .toast {
      position: fixed; right: 20px; bottom: 20px;
      z-index: 2147483645;
      display: none;
      align-items: center;
      gap: 10px;
      padding: 9px 16px;
      border-radius: 999px;
      border: 1px solid var(--c-border-bright);
      background: var(--c-surface);
      color: var(--c-text);
      font: 600 12px/1 'Inter', sans-serif;
      box-shadow: 0 8px 32px rgba(0,0,0,0.5), inset 0 1px 0 rgba(255,255,255,0.06);
    }
    .toast.active {
      display: flex;
      animation: toast-in 0.25s cubic-bezier(0.34,1.56,0.64,1) forwards;
    }
    .pulse {
      width: 7px; height: 7px;
      border-radius: 50%;
      background: var(--c-accent);
      animation: dot-pulse 1.8s ease-in-out infinite;
    }
    .count {
      border-left: 1px solid var(--c-border-bright);
      padding-left: 10px;
      color: var(--c-muted);
      font-size: 11px;
    }

    /* ── Hover Outline ───────────────────────────────────────── */
    .hover {
      position: fixed;
      z-index: 2147483644;
      pointer-events: none;
      display: none;
      border-radius: 8px;
      border: 1.5px solid rgba(99,102,241,0.6);
      box-shadow: 0 0 0 3px rgba(99,102,241,0.1), inset 0 0 20px rgba(99,102,241,0.04);
      transition: left 0.07s ease-out, top 0.07s ease-out, width 0.07s ease-out, height 0.07s ease-out;
    }

    /* ── Panel ───────────────────────────────────────────────── */
    .panel {
      position: absolute;
      z-index: 2147483646;
      width: 400px;
      display: none;
      flex-direction: column;
      background: var(--c-bg);
      border-radius: var(--radius);
      border: 1px solid rgba(99,102,241,0.22);
      box-shadow:
        0 0 0 1px rgba(99,102,241,0.07),
        0 24px 80px rgba(0,0,0,0.75),
        0 0 60px rgba(99,102,241,0.05),
        inset 0 1px 0 rgba(255,255,255,0.07);
      overflow: hidden;
    }
    .panel.visible {
      display: flex;
      animation: panel-in 0.22s cubic-bezier(0.34,1.1,0.64,1) forwards;
    }
    .panel.inspect-mode { width: 520px; }

    /* ── Header ──────────────────────────────────────────────── */
    .head {
      display: flex;
      align-items: center;
      justify-content: space-between;
      padding: 13px 14px;
      border-bottom: 1px solid var(--c-border);
      background: linear-gradient(180deg, rgba(99,102,241,0.07) 0%, transparent 100%);
      gap: 10px;
    }
    .head-left {
      display: flex;
      align-items: center;
      gap: 10px;
      min-width: 0;
    }
    .head-icon {
      width: 30px; height: 30px;
      border-radius: 9px;
      background: linear-gradient(135deg, var(--c-accent), var(--c-accent2));
      display: flex; align-items: center; justify-content: center;
      flex-shrink: 0;
      box-shadow: 0 4px 14px rgba(99,102,241,0.4);
    }
    .head-icon svg { width: 16px; height: 16px; fill: #fff; }
    .head-meta { min-width: 0; }
    .kind-chip {
      display: inline-flex;
      align-items: center;
      gap: 4px;
      font-size: 9.5px;
      font-weight: 700;
      color: var(--c-muted);
      text-transform: uppercase;
      letter-spacing: 0.09em;
      margin-bottom: 2px;
    }
    .kind-chip .dot {
      width: 5px; height: 5px;
      border-radius: 50%;
      background: var(--c-accent);
      display: inline-block;
      animation: dot-pulse 2s ease-in-out infinite;
    }
    .kind { display: inline; }
    .label {
      display: block;
      font-size: 13px;
      font-weight: 600;
      color: var(--c-text);
      white-space: nowrap;
      overflow: hidden;
      text-overflow: ellipsis;
      max-width: 215px;
    }
    .head-right {
      display: flex;
      align-items: center;
      gap: 6px;
      flex-shrink: 0;
    }

    /* ── Mode Tabs ───────────────────────────────────────────── */
    .mode-tabs {
      display: flex;
      background: var(--c-surface2);
      border-radius: 8px;
      padding: 2px;
      gap: 2px;
    }
    .tab-btn {
      display: flex;
      align-items: center;
      gap: 4px;
      padding: 4px 10px;
      border-radius: 6px;
      font-size: 11px;
      font-weight: 600;
      color: var(--c-muted);
      cursor: pointer;
      border: none;
      background: transparent;
      transition: color 0.15s, background 0.15s;
    }
    .tab-btn:hover { color: var(--c-text); }
    .tab-btn.active {
      background: var(--c-surface);
      color: var(--c-text);
      box-shadow: 0 1px 4px rgba(0,0,0,0.4), inset 0 1px 0 rgba(255,255,255,0.06);
    }
    .tab-btn.active.edit-tab { color: var(--c-amber); }

    /* ── Close Button ────────────────────────────────────────── */
    .close {
      width: 26px; height: 26px;
      display: flex; align-items: center; justify-content: center;
      border-radius: 7px;
      border: 1px solid var(--c-border);
      background: transparent;
      color: var(--c-muted);
      cursor: pointer;
      font-size: 15px;
      line-height: 1;
      transition: all 0.15s;
    }
    .close:hover {
      background: rgba(244,63,94,0.1);
      border-color: rgba(244,63,94,0.3);
      color: var(--c-red);
      transform: rotate(90deg);
    }

    /* ── Vision Badge ────────────────────────────────────────── */
    .vision-badge {
      display: inline-flex;
      align-items: center;
      gap: 4px;
      padding: 3px 8px;
      border-radius: 20px;
      font-size: 9.5px;
      font-weight: 700;
      letter-spacing: 0.07em;
      text-transform: uppercase;
      background: rgba(16,185,129,0.1);
      border: 1px solid rgba(16,185,129,0.25);
      color: var(--c-green);
      margin-bottom: 6px;
    }
    .vision-badge::before {
      content: '';
      width: 5px; height: 5px;
      border-radius: 50%;
      background: var(--c-green);
      display: inline-block;
      animation: dot-pulse 1.8s infinite;
    }

    /* ── Content / Answer ────────────────────────────────────── */
    .content-area {
      padding: 13px 14px;
      max-height: 260px;
      overflow-y: auto;
      display: flex;
      flex-direction: column;
      gap: 10px;
      scrollbar-width: thin;
      scrollbar-color: rgba(255,255,255,0.08) transparent;
    }
    .content-area::-webkit-scrollbar { width: 4px; }
    .content-area::-webkit-scrollbar-thumb { background: rgba(255,255,255,0.1); border-radius: 99px; }
    .answer {
      font-size: 13.5px;
      line-height: 1.68;
      color: #c9d1de;
      white-space: pre-wrap;
      animation: answer-in 0.2s ease forwards;
    }
    .answer:empty { display: none; }

    /* ── Shimmer Skeleton ────────────────────────────────────── */
    .skeleton { display: flex; flex-direction: column; gap: 8px; }
    .skel-line {
      height: 11px;
      border-radius: 6px;
      background: linear-gradient(90deg, var(--c-surface2) 25%, rgba(255,255,255,0.04) 50%, var(--c-surface2) 75%);
      background-size: 300px 100%;
      animation: shimmer 1.3s infinite;
    }
    .skel-line:nth-child(2) { width: 80%; }
    .skel-line:nth-child(3) { width: 55%; }

    /* ── Error ───────────────────────────────────────────────── */
    .error {
      display: none;
      align-items: flex-start;
      gap: 8px;
      padding: 10px 12px;
      border-radius: 10px;
      background: rgba(244,63,94,0.07);
      border: 1px solid rgba(244,63,94,0.18);
      color: #fca5a5;
      font-size: 12.5px;
      line-height: 1.55;
    }
    .error.visible { display: flex; }
    .error-icon { font-size: 14px; flex-shrink: 0; }

    /* ── Edit Controls ───────────────────────────────────────── */
    .edit-controls {
      display: none;
      gap: 6px;
      padding: 9px 14px;
      border-top: 1px solid var(--c-border);
      background: rgba(245,158,11,0.03);
    }
    .edit-controls.visible { display: flex; }
    .edit-btn {
      flex: 1;
      display: flex; align-items: center; justify-content: center; gap: 5px;
      padding: 7px 10px;
      border-radius: 8px;
      font-size: 11.5px;
      font-weight: 600;
      cursor: pointer;
      border: 1px solid var(--c-border-bright);
      background: var(--c-surface2);
      color: var(--c-muted);
      transition: all 0.15s;
    }
    .edit-btn:hover { background: var(--c-surface); color: var(--c-text); }
    .edit-btn.undo { border-color: rgba(245,158,11,0.2); color: var(--c-amber); }
    .edit-btn.undo:hover { background: rgba(245,158,11,0.08); }
    .edit-btn.copy { border-color: rgba(99,102,241,0.2); color: #a5b4fc; }
    .edit-btn.copy:hover { background: rgba(99,102,241,0.08); }
    .edit-btn:disabled { opacity: 0.35; cursor: not-allowed; }

    /* ── Footer / Input ──────────────────────────────────────── */
    .footer {
      padding: 11px 14px 14px;
      border-top: 1px solid var(--c-border);
      background: rgba(0,0,0,0.2);
    }
    form { display: flex; flex-direction: column; gap: 9px; }
    .input-row {
      display: flex;
      align-items: flex-end;
      gap: 8px;
      background: var(--c-surface);
      border: 1px solid var(--c-border-bright);
      border-radius: 12px;
      padding: 8px 8px 8px 12px;
      transition: border-color 0.2s, box-shadow 0.2s;
    }
    .input-row:focus-within {
      border-color: rgba(99,102,241,0.5);
      box-shadow: 0 0 0 3px rgba(99,102,241,0.1);
    }
    textarea {
      flex: 1;
      resize: none;
      min-height: 20px;
      max-height: 100px;
      border: none;
      background: transparent;
      color: var(--c-text);
      font: 13.5px/1.5 'Inter', sans-serif;
      outline: none;
      padding: 0;
      caret-color: var(--c-accent);
    }
    textarea::placeholder { color: var(--c-muted); }
    .submit {
      flex-shrink: 0;
      width: 34px; height: 34px;
      border-radius: 9px;
      border: none;
      background: linear-gradient(135deg, var(--c-accent), var(--c-accent2));
      cursor: pointer;
      display: flex; align-items: center; justify-content: center;
      transition: opacity 0.15s, transform 0.12s, box-shadow 0.2s;
      box-shadow: 0 4px 14px rgba(99,102,241,0.45);
    }
    .submit svg { width: 15px; height: 15px; fill: none; stroke: #fff; stroke-width: 2.2; stroke-linecap: round; stroke-linejoin: round; }
    .submit:hover:not(:disabled) { opacity: 0.88; transform: scale(1.06); box-shadow: 0 6px 20px rgba(99,102,241,0.6); }
    .submit:active:not(:disabled) { transform: scale(0.94); }
    .submit:disabled { opacity: 0.3; cursor: not-allowed; }
    .submit.edit-active {
      background: linear-gradient(135deg, var(--c-amber), #ef4444);
      box-shadow: 0 4px 14px rgba(245,158,11,0.45);
    }
    .hint {
      font-size: 10.5px;
      color: var(--c-muted);
      opacity: 0.65;
      display: block;
    }

    /* ── Inspect Styles ──────────────────────────────────────── */
    .inspect-data { font-family: 'Menlo','Monaco','Consolas',monospace; font-size: 11.5px; color: #cbd5e1; }
    .inspect-tag { color: #fbbf24; font-weight: 700; }
    .inspect-attr { display: block; margin: 3px 0; padding-left: 8px; border-left: 2px solid rgba(251,191,36,0.25); }
    .inspect-style { color: var(--c-muted); font-size: 11px; }
    .inspect-value { color: #cbd5e1; background: rgba(255,255,255,0.05); padding: 1px 4px; border-radius: 3px; }

    @media (max-width: 450px) {
      .panel { width: calc(100vw - 24px); left: 12px !important; }
    }
  </style>

    <div class="custom-cursor">
      <div class="cursor-orbit"></div>
      <div class="cursor-body"></div>
      <div class="cursor-dot"></div>
    </div>
    <div class="cursor-trail"></div>

    <div class="toast" role="status">
      <span class="pulse"></span>
      <span>ShakeAI active</span>
      <span class="count">0 shakes</span>
    </div>

    <div class="hover"></div>

    <section class="panel" aria-label="AI Overlay">
      <header class="head">
        <div class="head-left">
          <div class="head-icon">
            <svg viewBox="0 0 24 24"><path d="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm-1 14H9V8h2v8zm4 0h-2V8h2v8z"/></svg>
          </div>
          <div class="head-meta">
            <div class="kind-chip"><span class="dot"></span><span class="kind"></span></div>
            <span class="label"></span>
          </div>
        </div>
        <div class="head-right">
          ${t2.editEnabled ? '\n          <div class="mode-tabs">\n            <button class="tab-btn ask-tab active" type="button">\n              <svg width="10" height="10" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><circle cx="11" cy="11" r="8"/><path d="m21 21-4.35-4.35"/></svg>\n              Ask\n            </button>\n            <button class="tab-btn edit-tab" type="button">\n              <svg width="10" height="10" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><path d="M11 4H4a2 2 0 0 0-2 2v14c0 1.1.9 2 2 2h14a2 2 0 0 0 2-2v-7"/><path d="M18.5 2.5a2.121 2.121 0 0 1 3 3L12 15l-4 1 1-4 9.5-9.5z"/></svg>\n              Edit\n            </button>\n          </div>' : ""}
          <button class="close" type="button" aria-label="Close">&#x2715;</button>
        </div>
      </header>

      <div class="content-area">
        <div class="answer"></div>
        <div class="error"><span class="error-icon">&#9888;</span><span class="error-text"></span></div>
      </div>

      <div class="edit-controls">
        <button class="edit-btn undo" type="button">
          <svg width="11" height="11" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><path d="M9 14 4 9l5-5"/><path d="M4 9h10.5a5.5 5.5 0 0 1 0 11H11"/></svg>
          Undo
        </button>
        <button class="edit-btn copy" type="button">
          <svg width="11" height="11" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><rect x="9" y="9" width="13" height="13" rx="2"/><path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1"/></svg>
          Copy CSS
        </button>
      </div>

      <footer class="footer">
        <form>
          <div class="input-row">
            <textarea rows="1" placeholder="Ask anything about this…"></textarea>
            <button class="submit" type="submit" aria-label="Send">
              <svg viewBox="0 0 24 24"><line x1="22" y1="2" x2="11" y2="13"/><polygon points="22 2 15 22 11 13 2 9 22 2"/></svg>
            </button>
          </div>
          <span class="hint">Powered by ShakeAI</span>
        </form>
      </footer>
    </section>
  `;
  let a2 = i2.querySelector(".custom-cursor"), o2 = i2.querySelector(".cursor-trail"), s2 = i2.querySelector(".toast"), c2 = i2.querySelector(".count"), l2 = i2.querySelector(".hover"), u2 = i2.querySelector(".panel"), d2 = i2.querySelector(".kind"), f2 = i2.querySelector(".label"), p2 = i2.querySelector("form"), m2 = i2.querySelector("textarea"), h2 = i2.querySelector(".submit"), g2 = i2.querySelector(".close"), _2 = i2.querySelector(".error"), v2 = i2.querySelector(".error-text"), y2 = i2.querySelector(".answer"), b2 = i2.querySelector(".ask-tab"), x2 = i2.querySelector(".edit-tab"), S2 = i2.querySelector(".edit-controls"), C2 = i2.querySelector(".edit-btn.undo"), w2 = i2.querySelector(".edit-btn.copy");
  if (!a2 || !s2 || !c2 || !l2 || !u2 || !d2 || !f2 || !p2 || !m2 || !h2 || !g2 || !_2 || !y2)
    throw Error("AIOverlay UI failed to initialize");
  let T2 = false, E2 = (e3) => {
    T2 = e3, b2 == null ? void 0 : b2.classList.toggle("active", !e3), x2 == null ? void 0 : x2.classList.toggle("active", e3), h2.classList.toggle("edit-active", e3), m2.placeholder = e3 ? 'Describe the change… (e.g. "make background white")' : "Ask anything about this…";
  };
  b2 == null ? void 0 : b2.addEventListener("click", () => E2(false)), x2 == null ? void 0 : x2.addEventListener("click", () => E2(true)), C2 == null ? void 0 : C2.addEventListener("click", () => t2.onUndo()), w2 == null ? void 0 : w2.addEventListener("click", () => t2.onCopyCSS());
  let D2 = (e3) => {
    a2.style.left = `${e3.clientX}px`, a2.style.top = `${e3.clientY}px`, setTimeout(() => {
      o2 && (o2.style.left = `${e3.clientX}px`, o2.style.top = `${e3.clientY}px`);
    }, 80);
  };
  return p2.addEventListener("submit", (e3) => {
    e3.preventDefault();
    let n3 = m2.value.trim();
    n3 && t2.onSubmit(n3, T2);
  }), g2.addEventListener("click", () => {
    t2.onCancel();
  }), {
    root: n2,
    contains: (e3) => e3 === n2 || n2.contains(e3),
    setActive: (e3, t3) => {
      s2.classList.toggle("active", e3), a2.classList.toggle("active", e3), o2 == null ? void 0 : o2.classList.toggle("active", e3), c2.textContent = `${t3} shake${t3 === 1 ? "" : "s"}`, e3 ? window.addEventListener("mousemove", D2) : window.removeEventListener("mousemove", D2);
    },
    setHoverRect: (e3) => {
      if (!e3) {
        l2.style.display = "none";
        return;
      }
      l2.style.display = "block", l2.style.left = `${e3.left}px`, l2.style.top = `${e3.top}px`, l2.style.width = `${e3.width}px`, l2.style.height = `${e3.height}px`;
    },
    showPrompt: (t3) => {
      d2.textContent = t3.kind, f2.textContent = t3.label, m2.value = "", h2.disabled = false, h2.classList.toggle("edit-active", T2), _2.classList.remove("visible"), y2.textContent = "", S2 == null ? void 0 : S2.classList.remove("visible");
      let n3 = e(t3.rect.left + t3.rect.width / 2 - 190, 16, window.innerWidth - 396), r3 = t3.rect.bottom + window.scrollY + 12;
      if (u2.style.left = `${n3}px`, u2.style.top = `${r3}px`, u2.classList.add("visible"), t3.kind === "image" && t3.mimeType) {
        let e3 = d2;
        if (!e3.querySelector(".vision-badge")) {
          let t4 = document.createElement("span");
          t4.className = "vision-badge", t4.textContent = "Vision Enabled", e3.insertBefore(t4, e3.firstChild);
        }
      }
      m2.focus();
    },
    setThinking: (e3) => {
      h2.disabled = e3, h2.classList.toggle("edit-active", !e3 && T2), e3 && (y2.innerHTML = '<div class="skeleton"><div class="skel-line"></div><div class="skel-line"></div><div class="skel-line"></div></div>');
    },
    setAnswer: (e3) => {
      h2.disabled = false, y2.textContent = e3;
    },
    setError: (e3) => {
      v2 && (v2.textContent = e3), _2.classList.toggle("visible", !!e3);
    },
    clearSelection: () => {
      u2.classList.remove("visible"), _2.classList.remove("visible"), y2.textContent = "", m2.value = "", S2 == null ? void 0 : S2.classList.remove("visible");
      let e3 = d2.querySelector(".vision-badge");
      e3 && e3.remove();
    },
    destroy: () => {
      r2.remove(), n2.remove(), window.removeEventListener("mousemove", D2);
    },
    setInspectMode: (e3, t3) => {
      e3 ? (document.body.classList.add("ai-inspect-mode"), n2.classList.add("inspect-active"), t3 && u2 && (u2.classList.remove("visible"), u2.classList.add("inspect-mode"), F(u2, t3))) : (document.body.classList.remove("ai-inspect-mode"), n2.classList.remove("inspect-active"), I(u2), u2 && (u2.classList.add("visible"), u2.classList.remove("inspect-mode"), m2.focus()));
    },
    showEditControls: (e3) => {
      S2 && (S2.classList.add("visible"), C2 && (C2.disabled = !e3));
    }
  };
}
function F(e3, t2) {
  var _a;
  let n2 = document.createElement("div");
  n2.className = "inspect-data";
  let r2 = document.createElement("div");
  if (r2.innerHTML = `<span class="inspect-tag">&lt;${t2.tagName}&gt;</span> ${L(t2)}`, n2.appendChild(r2), t2.className) {
    let e4 = t2.className.split(" ").filter(Boolean).slice(0, 3).join(", "), r3 = document.createElement("div");
    r3.textContent = `Classes: ${e4}`, n2.appendChild(r3);
  }
  if (t2.id) {
    let e4 = document.createElement("div");
    e4.innerHTML = `<span class="inspect-attr">ID:</span> <code class="inspect-value">${R(t2.id)}</code>`, n2.appendChild(e4);
  }
  if (t2.classList.contains("selected")) {
    let e4 = document.createElement("div");
    e4.innerHTML = '<span class="inspect-attr">Status:</span> <strong style="color:#10b981">● Selected</strong>', n2.appendChild(e4);
  }
  let i2 = {};
  if ([
    "display",
    "position",
    "width",
    "height",
    "margin",
    "padding",
    "font-size",
    "color"
  ].forEach((e4) => {
    let n3 = getComputedStyle(t2).getPropertyValue(e4);
    n3 && !i2[e4] && (i2[e4] = n3);
  }), Object.keys(i2).length > 0) {
    let e4 = document.createElement("div");
    e4.innerHTML = "<strong>Computed Styles:</strong>", n2.appendChild(e4), Object.entries(i2).slice(0, 5).forEach(([e5, t3]) => {
      let r3 = document.createElement("div");
      r3.className = "inspect-style", r3.innerHTML = `<span class="inspect-attr">${R(e5)}:</span> <code class="inspect-value">${R(t3)}</code>`, n2.appendChild(r3);
    });
  }
  let a2 = {};
  if ((_a = t2.getAttributeNames()) == null ? void 0 : _a.forEach((e4) => {
    let n3 = t2.getAttribute(e4);
    n3 && !a2[e4] && (a2[e4] = n3);
  }), Object.keys(a2).length > 0) {
    let e4 = document.createElement("div");
    e4.innerHTML = "<strong>Attributes:</strong>", n2.appendChild(e4), Object.entries(a2).slice(0, 5).forEach(([e5, t3]) => {
      let r3 = document.createElement("div");
      r3.className = "inspect-attr", r3.innerHTML = `<span class="inspect-attr">${R(e5)}:</span> <code class="inspect-value">${R(t3)}</code>`, n2.appendChild(r3);
    });
  }
  let o2 = e3.querySelector(".content-area");
  o2 && o2.appendChild(n2);
}
function I(e3) {
  if (!e3)
    return;
  let t2 = e3.querySelector(".inspect-data");
  t2 && t2.remove();
}
function L(e3) {
  var _a, _b;
  let t2 = [(_a = e3.getAttribute("aria-label")) == null ? void 0 : _a.slice(0, 50), (_b = e3.getAttribute("title")) == null ? void 0 : _b.slice(0, 40)], n2 = (e3.innerText || "").slice(0, 80).trim();
  return n2 && !t2.some((e4) => e4) && t2.push(n2), t2.find(Boolean) || "";
}
function R(e3) {
  let t2 = document.createElement("div");
  return t2.textContent = e3, t2.innerHTML;
}
var z = class e2 {
  static getComputedStyles(e3) {
    let t2 = getComputedStyle(e3);
    return {
      display: t2.display,
      position: t2.position,
      visibility: t2.visibility,
      overflow: t2.overflow,
      width: t2.width,
      height: t2.height,
      minWidth: t2.minWidth,
      minHeight: t2.minHeight,
      maxWidth: t2.maxWidth,
      maxHeight: t2.maxHeight,
      margin: t2.margin,
      marginTop: t2.marginTop,
      marginRight: t2.marginRight,
      marginBottom: t2.marginBottom,
      marginLeft: t2.marginLeft,
      padding: t2.padding,
      paddingTop: t2.paddingTop,
      paddingRight: t2.paddingRight,
      paddingBottom: t2.paddingBottom,
      paddingLeft: t2.paddingLeft,
      border: t2.border,
      borderTop: t2.borderTop,
      borderRight: t2.borderRight,
      borderBottom: t2.borderBottom,
      borderLeft: t2.borderLeft,
      borderRadius: t2.borderRadius,
      borderWidth: t2.borderWidth,
      borderColor: t2.borderColor,
      backgroundColor: t2.backgroundColor,
      background: t2.background,
      color: t2.color,
      fontSize: t2.fontSize,
      fontWeight: t2.fontWeight,
      fontFamily: t2.fontFamily,
      lineHeight: t2.lineHeight,
      textAlign: t2.textAlign,
      textDecoration: t2.textDecoration,
      boxShadow: t2.boxShadow,
      zIndex: t2.zIndex,
      opacity: t2.opacity,
      cursor: t2.cursor
    };
  }
  static getAttributes(e3) {
    return Array.from(e3.attributes).reduce((e4, t2) => (e4[t2.name] = t2.value, e4), {});
  }
  static describeElement(t2) {
    var _a;
    let n2 = e2.getComputedStyles(t2), r2 = e2.getAttributes(t2);
    return {
      tagName: t2.tagName.toLowerCase(),
      className: (_a = t2.className) == null ? void 0 : _a.split(" ").filter(Boolean),
      id: t2.id || void 0,
      computedStyle: n2,
      attributes: r2,
      isEditable: t2.isContentEditable || ["INPUT", "TEXTAREA"].includes(t2.tagName)
    };
  }
  static getHierarchy(e3, t2 = 3) {
    let n2 = [e3];
    for (let r2 = 0; r2 < t2 && e3.parentElement; r2++)
      e3 = e3.parentElement, e3 && n2.push(e3);
    return n2;
  }
  static getFullStyles(e3) {
    let t2 = getComputedStyle(e3), n2 = {};
    [
      "display",
      "position",
      "visibility",
      "overflow",
      "width",
      "height",
      "margin",
      "padding",
      "border",
      "borderRadius",
      "borderWidth",
      "borderColor",
      "backgroundColor",
      "background",
      "color",
      "fontSize",
      "fontWeight",
      "fontFamily",
      "lineHeight",
      "textAlign",
      "textDecoration",
      "boxShadow",
      "zIndex",
      "opacity",
      "cursor"
    ].forEach((e4) => {
      let r2 = t2.getPropertyValue(e4);
      r2 !== "" && (n2[e4] = r2);
    });
    for (let e4 = 0; e4 < t2.length; e4++) {
      let r2 = t2[e4], i2 = t2.getPropertyValue(r2);
      i2 && !n2[r2] && (n2[r2] = i2);
    }
    return n2;
  }
  static matchesSelector(e3, t2) {
    return t2.some((t3) => e3.matches(t3) || e3.closest(t3) !== null);
  }
  static getAncestors(e3) {
    let t2 = [], n2 = e3.parentElement;
    for (; n2 && n2.tagName.toLowerCase() !== "html"; )
      t2.push(n2), n2 = n2.parentElement;
    return t2;
  }
  static create(t2 = document) {
    return new Proxy(e2, { get(e3, t3) {
      return Reflect.get(this, t3);
    } });
  }
  static analyze(t2) {
    let n2 = e2.describeElement(t2), r2 = [];
    return n2.computedStyle && n2.computedStyle.display === "block" && !n2.computedStyle.width && r2.push({
      type: "style",
      selector: `${t2.tagName.toLowerCase()}${t2.id ? "#" + t2.id : ""}`,
      explanation: "Element has implicit width from content. Consider adding explicit width for precise layout control."
    }), n2.computedStyle && n2.computedStyle.color !== "#000000" && r2.push({
      type: "style",
      selector: `${t2.tagName.toLowerCase()}${t2.id ? "#" + t2.id : ""}`,
      explanation: `Current color is ${n2.computedStyle.color}. This can be adjusted for accessibility or theming.`
    }), n2.attributes && !n2.attributes.onmouseover && !n2.attributes.onmouseenter && r2.push({
      type: "attribute",
      selector: `${t2.tagName.toLowerCase()}${t2.id ? "#" + t2.id : ""}`,
      proposedValue: "onmouseover",
      currentValue: void 0,
      explanation: "No hover handler detected. Consider adding for interactive states."
    }), {
      data: n2,
      suggestions: r2
    };
  }
};
var B = {
  primaryColor: "#14b8a6",
  panelBackground: "rgba(255, 255, 255, 0.96)",
  textColor: "#111827",
  borderRadius: 8
};
var V = {
  shake: true,
  keyboardShortcut: "mod+k"
};
var H = {
  text: true,
  images: true,
  elements: true,
  blockedSelectors: [
    'input[type="password"]',
    "[data-ai-private]",
    "[data-ai-overlay-ignore]"
  ],
  allowedSelectors: []
};
var U = class {
  constructor(e3) {
    __publicField(this, "active", false);
    __publicField(this, "shakeCount", 0);
    __publicField(this, "selection", null);
    __publicField(this, "inspectMode", false);
    __publicField(this, "disposers", []);
    __publicField(this, "ui");
    __publicField(this, "config");
    __publicField(this, "trigger");
    this.config = e3, this.trigger = {
      ...V,
      ...e3.trigger
    };
    let t2 = {
      ...H,
      ...e3.selection
    };
    this.ui = P({
      theme: {
        ...B,
        ...e3.theme
      },
      editEnabled: e3.editEnabled ?? false,
      onSubmit: (e4, t3) => {
        t3 ? this.applyAIEdit(e4) : this.ask(e4);
      },
      onCancel: () => {
        this.clearSelection(), this.inspectMode && this.deactivateInspectMode();
      },
      onUndo: () => {
        let e4 = h();
        e4 && this.ui.setAnswer(e4);
      },
      onCopyCSS: () => {
        let e4 = _();
        navigator.clipboard.writeText(e4).then(() => {
          this.ui.setAnswer("CSS copied to clipboard!");
        });
      }
    }), this.trigger.shake && this.disposers.push(w({ onShake: () => {
      this.shakeCount += 1, this.activate();
    } })), this.disposers.push(C({
      config: t2,
      isActive: () => this.active,
      isOverlayElement: (e4) => this.ui.contains(e4),
      onHover: (e4) => {
        this.ui.setHoverRect(this.selection ? null : e4);
      },
      onSelection: (t3) => {
        var _a, _b, _c;
        this.selection = t3, this.ui.setHoverRect(null), t3.kind === "image" && ((_a = e3.model) == null ? void 0 : _a.visionEnabled), this.ui.showPrompt(t3), (_c = (_b = this.config).onSelection) == null ? void 0 : _c.call(_b, t3);
      }
    })), this.disposers.push(this.bindKeyboardShortcut()), document.addEventListener("keydown", (e4) => {
      !this.active || e4.key !== "i" || this.toggleInspectMode();
    });
  }
  activate() {
    var _a, _b;
    if (this.active) {
      this.ui.setActive(true, this.shakeCount);
      return;
    }
    this.active = true, document.body.classList.add("ai-overlay-active"), this.ui.setActive(true, this.shakeCount), (_b = (_a = this.config).onActivate) == null ? void 0 : _b.call(_a);
  }
  deactivate() {
    var _a, _b;
    this.active && (this.active = false, this.clearSelection(), this.ui.setHoverRect(null), this.ui.setActive(false, this.shakeCount), document.body.classList.remove("ai-overlay-active"), (_b = (_a = this.config).onDeactivate) == null ? void 0 : _b.call(_a));
  }
  destroy() {
    for (let e3 of this.disposers)
      e3();
    this.disposers = [], this.deactivate(), this.ui.destroy();
  }
  isActive() {
    return this.active;
  }
  getInspectMode() {
    return this.inspectMode;
  }
  toggleInspectMode() {
    var _a, _b;
    this.inspectMode = !this.inspectMode, this.inspectMode ? (document.body.classList.add("ai-inspect-mode"), (_b = (_a = this.config).onInspect) == null ? void 0 : _b.call(_a, {
      element: null,
      inspectionData: null
    })) : (document.body.classList.remove("ai-inspect-mode"), this.deactivateInspectMode());
  }
  clearSelection() {
    var _a;
    this.selection = null, this.ui.clearSelection(), (_a = window.getSelection()) == null ? void 0 : _a.removeAllRanges();
  }
  async ask(e3) {
    var _a, _b, _c, _d, _e, _f, _g;
    if (!this.selection)
      return;
    let t2 = {
      question: e3,
      selection: this.selection
    };
    (_b = (_a = this.config).onAsk) == null ? void 0 : _b.call(_a, t2), this.ui.setThinking(true), this.ui.setAnswer(""), this.ui.setError("");
    try {
      let e4;
      e4 = this.selection.kind === "image" && ((_c = this.config.model) == null ? void 0 : _c.visionEnabled) ? await j(this.config.model ?? {}, t2) : this.config.siteKey ? await A({
        apiBaseUrl: this.config.apiBaseUrl,
        siteKey: this.config.siteKey,
        payload: t2
      }) : await O(this.config.model ?? {}, t2), this.ui.setAnswer(e4), (_e = (_d = this.config).onResponse) == null ? void 0 : _e.call(_d, e4, t2);
    } catch (e4) {
      let t3 = e4 instanceof Error ? e4 : Error("Unknown AIOverlay error");
      this.ui.setError(t3.message), (_g = (_f = this.config).onError) == null ? void 0 : _g.call(_f, t3);
    } finally {
      this.ui.setThinking(false);
    }
  }
  async applyAIEdit(e3) {
    var _a, _b, _c, _d;
    if (!this.selection)
      return;
    let t2 = this.selection.element;
    if (!t2) {
      this.ui.setError("No element selected for editing.");
      return;
    }
    this.ui.setThinking(true), this.ui.setAnswer(""), this.ui.setError("");
    try {
      let n2 = this.selection.cssSelector ?? f(t2), r2 = this.selection.computedStyles ?? p(t2), i2 = await N(this.config.model ?? {}, {
        selector: n2,
        tagName: t2.tagName.toLowerCase(),
        classes: t2.className || "",
        computedStyles: r2,
        innerTextSnippet: (t2.innerText ?? "").slice(0, 200)
      }, e3);
      m(i2), this.ui.setAnswer(`✅ ${i2.description}`), this.ui.showEditControls(g()), (_b = (_a = this.config).onEdit) == null ? void 0 : _b.call(_a, i2);
    } catch (e4) {
      let t3 = e4 instanceof Error ? e4 : Error("Edit failed");
      this.ui.setError(t3.message), (_d = (_c = this.config).onError) == null ? void 0 : _d.call(_c, t3);
    } finally {
      this.ui.setThinking(false);
    }
  }
  async inspectElement(e3) {
    var _a, _b, _c;
    try {
      let t2 = {
        element: e3,
        inspectionData: {
          tagName: e3.tagName.toLowerCase(),
          className: (_a = e3.className) == null ? void 0 : _a.split(" ").filter(Boolean),
          id: e3.id || void 0,
          computedStyle: z.getComputedStyles(e3),
          attributes: z.getAttributes(e3),
          isEditable: e3.isContentEditable || ["INPUT", "TEXTAREA"].includes(e3.tagName)
        }
      };
      return (_c = (_b = this.config).onInspect) == null ? void 0 : _c.call(_b, t2), setTimeout(() => {
        this.active && this.deactivateInspectMode();
      }, 1e4), t2;
    } catch (e4) {
      return console.warn("Inspection failed:", e4), null;
    }
  }
  deactivateInspectMode() {
    var _a, _b;
    this.inspectMode = false, document.body.classList.remove("ai-inspect-mode"), (_b = (_a = this.config).onDeactivate) == null ? void 0 : _b.call(_a);
  }
  bindKeyboardShortcut() {
    let e3 = this.trigger.keyboardShortcut.toLowerCase(), t2 = (t3) => {
      let n2 = e3.includes("mod+"), i2 = e3.split("+").at(-1), a2 = r() ? t3.metaKey : t3.ctrlKey;
      n2 && !a2 || i2 && t3.key.toLowerCase() === i2 && (t3.preventDefault(), this.active ? this.deactivate() : this.activate());
    };
    return document.addEventListener("keydown", t2), () => {
      document.removeEventListener("keydown", t2);
    };
  }
};
var W = {
  init(e3 = {}) {
    return new U(e3);
  },
  async inspectAt(e3) {
    let t2 = document.elementFromPoint(e3.clientX, e3.clientY);
    return !t2 || !document.contains(t2) ? null : new U({ model: {} }).inspectElement(t2);
  },
  inspector: z
};
typeof window < "u" && (window.AIOverlay = W);
var G = document.createElement("style");
G.textContent = "\n  body.ai-inspect-mode * {\n    cursor: crosshair !important;\n  }\n  \n  .ai-inspect-highlight {\n    outline: 3px solid #fbbf24 !important;\n    outline-offset: -3px;\n    border-radius: 4px;\n  }\n", document.head.append(G);
export {
  W as AIOverlay,
  z as ElementInspector
};
//# sourceMappingURL=@emmaexcel_shakecursor.js.map
